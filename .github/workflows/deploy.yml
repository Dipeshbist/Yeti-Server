name: Build & Deploy Yeti Backend (Dockerized)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    name: Build & Run on Server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create .env file from GitHub Secret
        working-directory: /home/ubuntu/yeti/yeti-backend/actions-runner/_work/Yeti-Server/Yeti-Server
        run: |
          echo "${{ secrets.ENV }}" > .env
          echo "✅ .env file created with environment variables"

      - name: Stop existing containers
        working-directory: /home/ubuntu/yeti/yeti-backend/actions-runner/_work/Yeti-Server/Yeti-Server
        run: |
          echo "🧹 Cleaning up old containers..."
          docker compose down --timeout 60 || true

      - name: Build Docker Compose services
        working-directory: /home/ubuntu/yeti/yeti-backend/actions-runner/_work/Yeti-Server/Yeti-Server
        run: |
          echo "🏗️ Building updated Docker images..."
          docker compose build

      - name: Start Docker Compose stack
        working-directory: /home/ubuntu/yeti/yeti-backend/actions-runner/_work/Yeti-Server/Yeti-Server
        run: |
          echo "🚀 Starting stack (Yeti + ThingsBoard)..."
          docker compose up -d

      - name: Wait for PostgreSQL
        run: |
          echo "⏳ Waiting for PostgreSQL to initialize..."
          sleep 45
          docker ps

      - name: Initialize ThingsBoard Database
        working-directory: /home/ubuntu/yeti/yeti-backend/actions-runner/_work/Yeti-Server/Yeti-Server
        run: |
          TB_VOLUME_EXISTS=$(docker volume ls -q -f name=tb-data)
          if [ -z "$TB_VOLUME_EXISTS" ]; then
            echo "🧩 Running ThingsBoard installation..."
            docker compose run --rm -e INSTALL_TB=true -e LOAD_DEMO=true thingsboard-ce || \
              echo "⚠️ Installation skipped — ThingsBoard already initialized."
          else
            echo "✅ ThingsBoard already initialized; skipping."
          fi

      - name: Verify Deployment
        run: |
          echo "🩺 Checking containers..."
          docker ps
          echo "🔍 Checking Yeti backend..."
          curl -f http://localhost:8000 || echo "⚠️ Backend not responding yet"
          echo "🌐 Checking ThingsBoard..."
          curl -f http://localhost:8080 || echo "⚠️ ThingsBoard UI check failed"

      - name: Show Docker Logs (on failure)
        if: failure()
        run: |
          echo "📜 Showing recent logs for debugging..."
          docker compose logs --tail=50
