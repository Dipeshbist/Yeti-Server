name: Build & Deploy Yeti Backend (Dockerized)

on:
  push:
    branches: [ main ]
  workflow_dispatch: # allow manual run

jobs:
  deploy:
    name: Build & Run on Server
    runs-on: self-hosted   # your Ubuntu runner that has Docker installed

    steps:
      # 1️⃣ Checkout latest code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Build Docker image for Yeti Backend
      - name: Build Docker image
        run: |
          docker build -t yeti-backend:latest -f Dockerfile .

      # 3️⃣ Stop & remove old container (if running)
      - name: Stop old container
        run: |
          if [ "$(docker ps -aq -f name=yeti-backend)" ]; then
            echo "🛑 Stopping old container..."
            docker stop yeti-backend || true
            docker rm yeti-backend || true
          fi

      # 4️⃣ Run the new container
      - name: Run updated container
        run: |
          echo "🚀 Starting new container..."
          docker run -d \
            --name yeti-backend \
            --restart=always \
            -p 8000:8000 \
            --env-file /root/deployments/yeti-backend/.env \
            yeti-backend:latest

      # 5️⃣ Health check
      - name: Verify deployment
        run: |
          echo "🩺 Checking status..."
          docker ps
          sleep 8
          curl -f http://localhost:8000 || echo "⚠️ Health check failed, but container started"
