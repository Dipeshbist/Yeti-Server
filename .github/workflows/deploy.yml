name: Deploy NestJS App

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code from GitHub
    - name: Checkout code
      uses: actions/checkout@v4

    # 2️⃣ Setup Node.js environment
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    # 3️⃣ Install dependencies and build locally (verifies build works)
    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build

    # 4️⃣ Setup SSH connection
    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Add server to known hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
        chmod 644 ~/.ssh/known_hosts

    # 5️⃣ Deploy to server
    - name: Deploy to server
      env:
        SSH_AUTH_SOCK: ${{ env.SSH_AUTH_SOCK }}
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} << 'EOF'
          set -e  # stop script if any command fails

          cd /root/deployments/yeti-backend || mkdir -p /root/deployments/yeti-backend
          cd /root/deployments/yeti-backend

          if [ ! -d "Yeti-Server" ]; then
            echo "📦 Cloning repository for the first time..."
            git clone https://github.com/Dipeshbist/Yeti-Server.git
          fi

          cd Yeti-Server

          echo "📥 Pulling latest code from main..."
          git fetch origin main
          git reset --hard origin/main

          echo "⚙️ Installing dependencies..."
          npm ci

          echo "🏗️ Building project..."
          npm run build

          echo "📘 Running Prisma setup..."
          npx prisma generate || echo "⚠️ Prisma client generation skipped"
          npx prisma migrate deploy || echo "⚠️ No pending migrations"

          echo "🚀 Restarting backend with PM2..."
          pm2 delete yeti-backend || true
          pm2 start dist/src/main.js --name yeti-backend
          pm2 save

          echo "✅ Deployment completed successfully!"
        EOF

    # 6️⃣ Health Check
    - name: Health Check
      run: |
        echo "🩺 Running health check..."
        sleep 10
        curl -f http://${{ secrets.SSH_HOST }}:8000 || echo "⚠️ Health check failed, but continuing..."
